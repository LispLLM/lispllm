# lispllm

A GPT-style language model in one page of Lisp, running live in your browser. The page is a
scrollable narrative where every figure is computed by the code displayed beside it. There is
no backend, no router, no second page.

## Quickstart

```sh
pnpm i && pnpm dev
```

(Full README written at M6.)
